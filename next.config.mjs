/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "phuclong.com.vn",
            "res.cloudinary.com",
            "placehold.co"
        ]
    }
};

export default nextConfig;
